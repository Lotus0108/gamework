<h1>レビュー投稿フォーム</h1>
  <%= form_with model: @items,url: '/items', method: :post, locl:true do |f| %>
    <h4>商品名</h4>
      <%= f.text_field :title %>
    <h4>レビュー</h4>
      <%= f.text_area :body %>
    <%= f.submit '投稿' %>
  <% end %>